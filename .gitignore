__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.env
.venv
env/
venv/
ENV/
.pythonlibs/
uploads/
processed/
*.pdf
outputs/
training_images/
# 学習データの一時置き場（HF Datasetリポジトリへ送るためのローカルコピー）
training_data/
# 学習データを取り出したもの（tools/fetch_training_data.py の出力）
collected/
# 学習・検証用データ（重い。GitにもHF Spacesにも上げない）
datasets/

# Hubから落としたデータのキャッシュ（datasets/ で既に除外済みだが明示）
datasets/hub/

# tools/train_new_model.py の作業フォルダ。非公開の学習データ一式（hub/）と重みが入る。
# redeploy_hf.ps1 は git add -A で送るので、除外しないと公開Spaceに上がってしまう
training_run/
# ultralytics が project を相対パスで受けたときの既定の出力先
runs/
# ultralytics が学習前のAMPチェック用に落とす公式モデル（学習には使わない）
weights/
